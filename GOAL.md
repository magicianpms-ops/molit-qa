# GOAL — 국토부 질의회신 건축 안전·품질 스크랩 라이브러리

## 목표
국토부 계열 질의회신을 **건축 안전·품질 중심**으로 수집해, 원본 이미지 대신
**경량 MD(텍스트)** 로 저장하여 저장소 용량을 최소화하고, 필요할 때 즉시
검색·조회할 수 있는 개인용 라이브러리를 만든다. 검증 후 lh 앱에 MD 라이브러리로 통합.

## 대상 소스 (사용자 확정)
1. 국토부 민원마당 `eminwon.molit.go.kr` FAQ, 카테고리 건축(category=2102) — **구현·검증 완료**
2. 국민신문고 `epeople.go.kr` 민원/정책 질의응답 (건축 필터) — 예정
3. 세움터 `eais.go.kr` 건축법 전문질의 (SSO 게이트, 참고용) — 예정/보류

## 완료 기준 (체크 가능)
- [x] 소스①에서 목록→상세를 세션 유지하며 스크랩, 구조화 필드 추출
- [x] 질의/회신 + 메타(담당기관·부서·법령·일자)를 YAML frontmatter MD로 저장
- [x] 안전·품질 키워드 자동 태깅(sq_relevant, tags, matched_kw)
- [x] 재실행 시 hash 비교로 증분 갱신(중복 저장 방지)
- [x] 검색 CLI (태그/키워드/안전·품질 필터)
- [ ] 소스① 건축 전건(≈1,060건) 크롤 완료
- [ ] 소스② 어댑터
- [ ] 소스③ 어댑터(로그인 처리)
- [ ] (선택) LLM 재분류로 키워드 오탐 보정
- [ ] (선택) 원본 증빙용 저해상 썸네일/PDF 보존 옵션
- [ ] lh 통합(standards/*.md 방식으로 임포트)

## 불변 원칙
- 이미지 대신 텍스트 우선(용량 최소화). 원본 증빙은 URL + 선택적 썸네일.
- 정부 서버 예의: 요청 간 지연, 증분 크롤.
- 건축 전건 보존 + 안전·품질은 태그 필터로 노출(과도한 삭제 금지).
